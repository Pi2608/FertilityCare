services:
  - type: web
    name: fertilitycare-backend
    env: java
    buildCommand: |
      cd be
      chmod +x mvnw
      ./mvnw clean package
    startCommand: java -jar be/target/*.jar
    envVars:
      - key: JAVA_HOME
        value: /opt/render/project/.render/opt/java/openjdk-17
